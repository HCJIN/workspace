<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<mapper namespace="adminMapper">

    <!-- 카테고리 목록 조회 -->
    <select id="getItemCategory" resultMap="itemMapper.category" >
        SELECT CATE_CODE,
            CATE_NAME
        FROM ITEM_CATEGORY
    </select>

    <insert id="setItem" >
        INSERT INTO SHOP_ITEM (
            ITEM_NAME
            ,   ITEM_PRICE
            ,   ITEM_INTRO
            ,   CATE_CODE
        ) VALUES (
            #{itemName}
            ,   #{itemPrice}
            ,   #{itemIntro}
            ,   #{cateCode}
        );
    </insert>

    <select id="getItemList" resultMap="itemMapper.item">
        SELECT ITEM_CODE
        ,   ITEM_NAME
        ,   ITEM_PRICE
        ,   ITEM_INTRO
        ,   ITEM_STOCK
        ,   ITEM_STATUS
        ,   CATE_CODE
        FROM SHOP_ITEM;
    </select>

</mapper>































